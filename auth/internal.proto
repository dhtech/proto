syntax = "proto3";

package auth;

// Contains all facts known about the verified user
message VerifiedUser {
  string username = 1;

  string ip = 2;

  string port = 3;

  repeated string group = 4;
}
